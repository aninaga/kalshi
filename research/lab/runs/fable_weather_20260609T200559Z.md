# weather / H2 drift-fade (overreaction mirror) — DEAD

_Date: 2026-06-09. Branch: `claude/hedge-fund-strategy-analysis-jyXWB`.
Agent: `fable_weather`. Hypothesis id `ac6c9c02b539c041` (registered + claimed
2026-06-09T19:39:27Z, BEFORE any scoring).
Family: **weather**, market: **temp**. Test split never read._

## Motivation

The walking-skeleton run (`weather_skeleton_20260609T192323Z.md`,
hyp `8aabae5235c8ed07`) killed drift **continuation** decisively
(−6.21¢/ct, CI entirely below zero) and explicitly flagged the un-scored
mirror as a NEW hypothesis requiring its own pre-registration. This run
scores that mirror honestly: a continuation result of −6.21¢ under ~3.2¢ of
costs implies a *gross* fade edge of only ~+3¢ — i.e. the mirror was
predicted to be approximately cost-eaten, and the point of this trial is to
close the mechanism's second direction rather than leave a dangling
"maybe the flip works".

## Mechanism

Intraday OVERREACTION: thin daily-high books overshoot on intraday quote
moves driven by noise-trader flow; a recent implied-temp move partially
mean-reverts by settlement.

## Pre-registered signal + direction (fixed at registration)

Signal IDENTICAL to the dead mirror (no re-tuning): implied-temp drift over
the last 5 bars, |drift| ≥ 0.75°F, 1 h book warm-up, freshness ≤ 2 stale
minutes. Direction: **fade_the_drift** — drift > 0 → "under",
drift < 0 → "over". Hold to settlement. Execution:
`FillModel(venue="kalshi", half_spread=max(2 × 0.0075, 0.015) = 0.015)`,
honest i+1 latency.

## Gate numbers — REALISTIC fills

**TRAIN** (875 panels → 804 trades, cities balanced 154–168):
**−0.20¢/ct**, CI [−3.06, +2.73].

**FULL NON-TEST** (1,062 panels → 976 trades, 474 over / 502 under):
**−0.27¢/ct**, CI [−2.86, +2.35], n=976. Gate **failed**.

- Cost sweep (monotone): 0¢ → −0.27, 1¢ → −1.27, 2¢ → −2.27, 3¢ → −3.27,
  4¢ → −4.27.
- Walk-forward: +0.53, +3.54, +0.45, −0.16, +3.33, **−9.66 (2026-03)**,
  +1.11, −5.67 (n=6) — 5/8 months positive but none significant and one
  −9.7¢ blow-up month; no stability.
- Adversarial: staleness ✓, estimator-bias ✓ (win 0.583 vs fill 0.554 —
  honest fills), concentration ✓ (5 cities, max ~20.6%), OOS ✗ (season-half
  CIs don't overlap; late half worse).
- Decomposition: gross **+2.97¢** − spread 1.50¢ − Kalshi fee 1.74¢ =
  **−0.27¢**. The reversion is REAL (win rate 2.9¢ above price paid) but
  smaller than the cost of taking liquidity near p≈0.55, where the Kalshi
  fee curve peaks.

## Bug-hunt

Negative/near-zero result, so the hunt is for *false negatives* and
accounting errors rather than optimism: estimator-bias check passes (win
rate matches price + gross, so fills and settlement orientation are
consistent); drift uses only past bars (no look-ahead by construction);
cost sweep strictly monotone; the gross/cost decomposition closes to the
headline to the cent. Settlement orientation also independently verified by
the continuation/fade arithmetic: gross_fade (+2.97¢) ≈ −(continuation
−6.21¢ + 2×costs ~3.2¢), exactly as predicted before scoring.

## Independent collision (corroboration)

Sibling agent `opus_weather` registered the same mirror mechanism
(`af866e8ac7d19284`) 2.5 minutes AFTER this hypothesis was registered and
claimed, worked it concurrently, and reached numerically identical results
(−0.27¢, CI [−2.86, +2.35], n=976 — the substrate is deterministic) with
verdict DEAD. Two independent pre-registrations, one shared falsification.
Noted in both registry rows; the duplicate inflates the weather-family DSR
N conservatively (never weakens the hurdle).

## Verdict economics — the mechanism is now CLOSED in both directions

Continuation: −6.21¢ (CI < 0, decisively wrong). Fade: −0.27¢ (CI straddles
0; gross +2.97¢ < 3.24¢ costs). The thin daily-high book does mean-revert
slightly after a ≥0.75°F intraday move, but the inefficiency is smaller
than one taker round-trip. Nothing here survives realistic execution; any
revival must change the economics (maker fills, fee-free promos), not the
signal.

## Governance / ledger

Registry `ac6c9c02b539c041` → done/DEAD; analyst ledger row appended
(`family="weather"`, `market="temp"`, agent `fable_weather`).

## Reproduce

```bash
python -m research.scripts.weather_fable_h1h2 --hyp driftfade --split train
python -m research.scripts.weather_fable_h1h2 --hyp driftfade --split nontest
```

## VERDICT: DEAD
