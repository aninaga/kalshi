# weather / temp — drift-REVERSION (fade) mirror — FALSIFICATION

_Date: 2026-06-09. Branch: `claude/hedge-fund-strategy-analysis-jyXWB`.
Agent: `opus_weather`. Hypothesis id `af866e8ac7d19284`.
Family: **weather** (per-family DSR partition, N=1 cold start). Test split never read._

## EDA observation that motivated it

The walking-skeleton (`weather_skeleton_20260609T192323Z.md`, hyp
`8aabae5235c8ed07`) found intraday drift **continuation** decisively DEAD:
−6.21¢/ct on full non-test, CI entirely below zero. It explicitly flagged the
**un-scored mirror** (fade the drift) as a new, pre-registerable hypothesis.

I confirmed the mirror has a real signal before pre-registering. On TRAIN,
conditioning the ATM-boundary outcome on the recent 5-bar implied-temp drift
(first fresh bar after 1h warmup, |drift| ≥ 0.75°F, one obs/event):

| recent drift | ATM P(over) priced | realized P(over) | residual |
|--------------|-------------------|------------------|----------|
| UP   (n=408) | 0.456 | 0.382 | **−7.4¢** (market overshot up → bet UNDER) |
| DOWN (n=396) | 0.644 | 0.684 | **+4.0¢** (market overshot down → bet OVER) |

Both sides reject continuation and favor reversion. Distinct mechanism from the
DEAD continuation: thin daily-high books **over-extrapolate** a transient
implied-temp move and revert by settlement.

Note on scope: the FillModel snaps to the **listed strike nearest `mid`** (the
ATM boundary) and reads its real quoted prob — it does **not** trade arbitrary
wings. I separately checked the textbook favorite/longshot calibration (wings
overpriced ~0.1, favorites underpriced ~0.7-0.8) — real in the data but
**not harvestable through this strategy's ATM-only fill**, and the ATM boundary
itself is directionally well-calibrated (resid ≈ 0 at f=0.5/0.75). So the only
ATM-tradeable mechanism is the drift-reversion above.

## Pre-registered hypothesis (fixed before any scoring)

- **Direction:** `fade_the_drift` — UP drift → side `under`; DOWN drift → side `over`, at the ATM strike, held to settlement.
- **Signal:** implied-temp drift over last 5 fresh bars; fire at first fresh bar with |drift| ≥ 0.75°F after 1h warmup.
- **Execution:** honest i+1 latency; freshness ≤ 2 stale minutes;
  `FillModel(venue="kalshi", half_spread=0.015)` where
  0.015 = `max(2 × measured TRAIN bucket-median half-spread 0.0075, 0.015)`
  (measured median = 0.75¢/bucket on train; interior cumulative boundaries are
  multi-leg synthetics → charged 2 legs).

Registered + claimed (`opus_weather`) BEFORE scoring.

## Gate numbers — REALISTIC fills

**TRAIN** (875 panels → 804 trades): **−0.20¢/ct**, CI **[−3.06, +2.73]**.

**FULL NON-TEST** (1,062 panels → 976 trades): **−0.27¢/ct**, CI **[−2.86, +2.35]** — straddles zero, gate FAIL.

Cost sweep (monotone, honest signature):

| extra cost | net ¢/ct | CI lo | gate |
|-----------:|---------:|------:|------|
| +0¢ | −0.27 | −2.86 | False |
| +1¢ | −1.27 | −3.86 | False |
| +2¢ | −2.27 | −4.86 | False |
| +3¢ | −3.27 | −5.86 | False |
| +4¢ | −4.27 | −6.86 | False |

Walk-forward (monthly, non-test): 5/8 months positive but **none individually
significant**; one large negative month 2026-03 = −9.7¢. OOS season-half:
**FAIL** — H1 +1.18¢ vs H2 −1.72¢ (decays and inverts).

## The decisive decomposition (why it dies)

Per-trade, non-test:
- **Gross edge** (payoff − fill_mid): **+3.04¢** — the reversion is directionally REAL.
- **Cost** (half_spread 1.5¢ + Kalshi taker fee ~1.7¢): **−3.24¢**.
- **Net: −0.20¢.** The gross reversion is almost exactly eaten by realistic execution cost.

Side asymmetry (matches EDA): UNDER n=408 gross +4.53¢ → net +1.24¢; OVER n=396 gross +1.51¢ → net −1.69¢. Even the stronger UNDER leg's net is small and not gate-significant.

## Bug-hunt (hunted hard; the result is a clean true-negative, not an artifact)

1. **Estimator-bias / unfair strike-snap:** win-rate 0.584 vs avg price paid 0.554, |gap| = 3.0¢ < 5¢ → fills are HONEST; the edge is not manufactured by snapping to a far/mispriced strike. PASS.
2. **Settlement orientation:** verified by hand on sample trades — `under` pays iff realized ≤ strike, `over` iff realized > strike; all matched. No silent P&L inversion.
3. **Look-ahead:** drift uses only past bars (`mid[i] − mid[i-5]`); built-in i+1 latency; freshness guard ≤ 2 stale minutes. No future leakage.
4. **Cost artifact:** cost sweep monotone-decreasing, negative everywhere; gross/cost decomposition isolates the kill as execution drag, not a sign error.
5. **Concentration:** top game 0.3% of |pnl|, top team (CHI) 23.7% (< 25% cap). PASS — not a one-city fluke.
6. **Too-good check:** N/A — result is flat/negative, the opposite failure mode; the EDA's larger raw residual (−7.4¢) shrank to +3.04¢ gross under the FillModel's ATM strike-snap + clipping + i+1 latency, which is the expected honest erosion, not a bug.

## VERDICT: DEAD

The fade-drift mirror is **directionally correct** (gross +3.04¢, fills honest,
both legs reject continuation), but the reversion is **too small to clear
realistic execution cost** (~3.24¢ spread + Kalshi fee). Net −0.27¢/ct on full
non-test with a CI straddling zero and an OOS season-half that decays and
inverts. The thin daily-high book's intraday over-reaction is real but **priced
within the cost of trading it** at this horizon/threshold via the ATM boundary.

Together with the DEAD continuation, this closes the intraday-drift mechanism on
weather/temp in BOTH directions: continuation loses ~6¢ (it pays the cost AND
bets the wrong way), fade is ~flat (right way, but the edge ≈ the cost).

## Reproduce

```bash
# measured train half-spread median (-> FillModel floor):
python3 -c "import numpy as np; from research.lab import data; \
hs=np.concatenate([p.features['half_spread'][np.isfinite(p.features['half_spread'])] \
for p in data.load_panels('temp','train')]); print(np.median(hs))"   # 0.0075

# pre-registered fade-drift gate, both splits:
python -m research.scripts.weather_fade_drift_e2e --split train
python -m research.scripts.weather_fade_drift_e2e --split nontest
```

Strategy: `research/scripts/weather_fade_drift_e2e.py`
(DRIFT_BARS=5, DRIFT_THRESH=0.75, WARMUP_SEC=3600, HALF_SPREAD=0.015,
`FillModel(venue="kalshi")`, `evaluate(..., family="weather")`).

Registry `af866e8ac7d19284` updated → DEAD; analyst ledger row appended
(`family:"weather", market:"temp"`).

DEAD
