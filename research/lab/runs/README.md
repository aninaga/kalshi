# Agent run verdicts (durable)

One markdown per agent run: `<lane>_<utc-timestamp>.md`, written by background
Opus 4.8 agents under the supervisor policy (`research/lab/SUPERVISOR.md`). This
directory is tracked in git so verdicts survive a container reclaim.
