[pytest]
# Arb-bot tests are async (aiohttp clients); research-harness tests are sync.
# asyncio_mode=auto lets pytest-asyncio run the `async def` tests without a
# per-test decorator. Markers registered to silence unknown-mark warnings.
asyncio_mode = auto
markers =
    asyncio: mark a test as an asyncio coroutine
